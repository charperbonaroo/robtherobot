{
  "name": "rob-gui",
  "packageManager": "yarn@4.5.3",
  "type": "commonjs",
  "dependencies": {
    "bootstrap": "^5.3.3",
    "lit": "^3.2.1",
    "lodash": "^4.17.21",
    "marked": "^15.0.6",
    "rob-web": "workspace:*"
  },
  "scripts": {
    "build": "yarn type-check && yarn build:skip-types",
    "build:skip-types": "esbuild --sourcemap --bundle --loader:.png=file --outdir=dist src/index.ts",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bootstrap": "^5",
    "@types/lodash": "^4.17.13",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.2"
  }
}
